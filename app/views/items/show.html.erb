<h1>Item</h1>

<h2><%= @department.name %> - <%= @item.name %></h2>

<%= link_to 'Edit', edit_department_item_path([@department, @item]) %>
<%= link_to 'Delete', [@department, @item], method: :delete %>
<br></br> 
<%= link_to 'Back To Items', department_items_path(@department) %>